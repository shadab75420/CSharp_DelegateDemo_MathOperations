# CSharp_DelegateDemo_MathOperations
This project demonstrates the concept of delegates in C# using a simple mathematical operations example. A delegate is used to reference different methods (Add, Subtract, Multiply) dynamically at runtime.
